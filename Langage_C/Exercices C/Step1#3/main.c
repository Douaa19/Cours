#include <stdio.h>
#include <stdlib.h>

int main()
{
    int N, i, Som;
    float Moy;
    Som=0;
    printf("Entrer un nombre: ");
    scanf("%d", &N);
    while (N!=-1);{
    printf("Entrer un nombre: ");
    scanf("%d", N);
       if (N!=-1);
       Som=Som+N;
       }
    printf("La moyenne = %f", Moy);
    Moy=Som/i;


    return 0;
}
